namespace WeighBridge.Core.Theming;

/// <summary>The visual themes shipped by the design system.</summary>
public enum AppTheme
{
    /// <summary>Follow the Windows personalisation setting.</summary>
    System = 0,

    /// <summary>Light theme — the default for daylight cabin operation.</summary>
    Light = 1,

    /// <summary>Dark theme — reduces glare during night shifts.</summary>
    Dark = 2,
}
